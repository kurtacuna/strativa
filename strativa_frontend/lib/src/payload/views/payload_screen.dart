import 'package:flutter/material.dart';

class PayloadScreen extends StatelessWidget {
  const PayloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Pay/Load"))
    );
  }
}